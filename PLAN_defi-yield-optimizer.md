# defi-yield-optimizer 実行計画書 — フェーズ3「名目上」→実際の少額実行へ

**更新日: 2026-09-11(2)**(初版: 2026-09-10)

**前提**: フェーズ1(APYデータ取得)・フェーズ2(スコアリング+日次スナップショット自動蓄積)は
稼働済み。READMEはフェーズ3(小額実行)を「今ここ」としていたが、実際に資金を入れた
記録はコード・README上どこにも無かった。名目上のフェーズと実態にズレがあったため、
実弾投入前チェックとして以下を計画的に進めている。

もう一つの限界: バックテスト検証(311フォールド、20プール、11四半期)が示しているのは
「安定性フィルタ(apy_cv・TVL急減)には予測力の兆候がある」までであり、監査・ハック減点まで
含めた複合スコア全体の予測力はまだ実証されていない。このツールの本質は「エッジを取る」
のではなく「既存の公開利回りを、詐欺・ハックを踏まずに拾う」フィルターであることを
前提に計画する。

**ガードレール(Claude Codeが単独で越えてはいけない境界)**:
- 実際の送金・入金・スワップ・ウォレット操作は、常にユーザー本人が自分のウォレット
  (MetaMask等)で行う。Claude Codeはスコアリング・監視のコードを整備するのみ。
- 秘密鍵・シードフレーズはコードにも`.env`にも一切書かない(README既定方針を維持)。
- 監査レポート原本・コントラクトアドレスの一致確認など「人間の目による二次確認」は
  Claude Codeが代行できない。これは明示的にユーザー本人のタスクとして切り分ける。
- 各フェーズ完了時に一度ユーザーに報告し、次に進む意思を確認する。
  特にフェーズB→C(実際の入金)の移行は必ず確認を挟む。
- **(2026-09-11追加)** push・PRのマージなど不可逆に近いGit操作も、都度ユーザーの
  明示的な合意を得てから行う。

---

## フェーズA: 最新データでの再判定 — 完了(2026-09-11)

- [x] `python src/scoring.py`を最新データで実行し、現時点の上位候補リスト(`ok`フラグ7件)を取得
- [x] 前々回・前回とランキングが入れ替わっていることを確認。前回検討していた
      `zerobase-cedefi`は今回は候補40件からも外れていた(「必ずその場の最新データで
      判断する」の重要性を実地で確認)
- [x] `data/snapshots/`の蓄積状況を確認 → **不具合を発見・修正**
      - 2026-09-08の1件のみで3日間停止していた
      - 原因: タスクスケジューラの既定設定(`StartWhenAvailable=False`・バッテリー駆動不可・
        スリープ時不可)により、08:00にPCがログオン状態でないと無言でスキップされ、
        追いつく仕組みも無かった
      - `StartWhenAvailable`/`WakeToRun`を有効化、バッテリー制限を解除して修正
      - 2026-09-11分のスナップショットを追加取得し、動作確認済み
      - コミット済み(`50451f0`)・ブランチ→PR→マージ済み([PR #4](https://github.com/intel-core-hub/defi-yield-optimizer/pull/4)、2026-09-11)

## フェーズB: 二次確認(監査対応・実コントラクト確認)

2026-09-11、ユーザーから「フェーズB監査対応 修正計画書」(pool_id単位でコントラクト・
監査・権限の対応関係を固定するための詳細仕様)を受領。方向性は採用しつつ、以下の
役割分担で着手した。

**Claude Codeが実施(客観データの収集・仕組み整備のみ、承認判断は行わない)**:
- [x] `docs/phase-b/README.md` — manifestスキーマと運用ルールを新設
- [x] `docs/phase-b/2026-09-11.yaml` — 対象7プールを`selection_snapshot`で固定し、
      各プールの調査結果を記録するmanifestを作成。全プール`phase_b_status: pending`。
      - Pendle V2の5プール(STRUSD/REUSD/USD3×2/SUSDAI)は、Pendle公開API
        (`api-v2.pendle.finance`)からMarket/SY/PT/YT/expiryアドレスを取得し、
        DeFiLlamaの`underlyingTokens`と完全一致することを確認済み(pool_id取り違えの
        可能性は排除できた)
      - Ember(Sui)・Unitas(BSC)は、簡単に自動取得できる公開APIが見つからず、
        実装アドレス・権限周りは`null`のまま「要確認」として残した(埋めていない
        ものを埋めたように見せない)
- [x] `src/phase_b.py` — manifestを読み込み、`phase_b_status`が`approved`以外の
      プールをスコア対象(NaN)から除外する`apply_phase_b_gate()`を実装。manifestに
      無い`pool_id`も`not_reviewed`として同様に除外(保守的に倒す)
- [x] `python src/scoring.py <額> --phase-b docs/phase-b/YYYY-MM-DD.yaml`で
      Phase Bゲートを適用できるようにCLIを拡張(既存の引数なし実行は非破壊)
- [x] `analyze_top_candidates()`内の履歴取得失敗が無言でスキップされていた箇所を
      `logging.warning`で可視化(テスト追加、44件全通過)
- [ ] (先送り) CI強制チェック・オンチェーンbytecode/proxy/admin role自動検証
      スクリプト・リスクモデルのプール単位ツリー化 — manifestに実データが埋まって
      からの方が土台として適切と判断し、今回は着手していない

**ユーザー本人のタスク(Claude Codeが代行できない・してはいけない部分)**:
- [ ] 監査レポート原本(Pendle Core、Ember、ScaleBit/SlowMist)を開き、Critical/High
      指摘が実際に解消されているか、監査scopeが今回のMarket/SY実装を含むかを確認
- [ ] Pendle 5件について、各SY実装・underlying発行体(strUSD/reUSD/USD3/sUSDai)固有の
      監査状況を確認(Pendle Core監査だけでは不十分という指摘は妥当)
- [ ] Ember(Sui)のvault object・package revisionの特定、Unitas(BSC)の
      proxy/implementation・privileged roleの確認
- [ ] 確認が取れたプールから、`docs/phase-b/2026-09-11.yaml`の該当`phase_b_status`を
      `approved`に更新(このファイルへの書き込みはいつでも代行するが、`approved`と
      判断する行為自体はユーザー本人が行う)

**→ 上記ユーザータスクが完了し`approved`になったプールのみが、`--phase-b`指定時の
配分候補になる。**

## フェーズC: 少額実行(ユーザー本人の操作) — 未着手

- [ ] 「失っても困らない額」(1万円と述べていた額)を確定
- [ ] テストネットが選んだプロトコル/チェーンで利用可能か確認。難しければ
      そのまま少額の実弾で開始
- [ ] 送金・入金・スワップはユーザー本人が自分のウォレットで実行
- [ ] 実行後、実際に入れた額・対象プール・pool_id・日付を記録する場所を用意
      (シンプルなCSVかMarkdownの実行ログ程度で十分)

## フェーズD: モニタリング体制の整備 — 未着手(フェーズAの修正で土台は前倒し着手済み)

- [x] `src/snapshot.py`の日次自動蓄積が止まっていた問題を修正(フェーズAで実施)
- [ ] 入金後も継続して動いているかを定期確認できる仕組みを用意
      (ログファイルの異常終了検知程度で十分、過剰な通知システムは不要)
- [ ] `src/scoring.py`を定期的に再実行し、保有中プールのスコア・フラグに変化がないか
      (特に`past_hack_risk`への転落)を監視する運用フローを明文化
- [ ] 最低数週間は増額しない方針を維持しつつ、想定通りのAPY/TVLの動きになっているかを
      フェーズCで作った実行ログと突き合わせて確認

## フェーズE: 検証モデルの継続強化(並行して進める中長期タスク) — 未着手

現状の裏付けは「安定性フィルタ単体」までで、複合スコア全体の予測力は未検証。
これは実行の妨げにはならないが、次に貯まったデータで着手すべき改善:

- [ ] `data/snapshots/`が数ヶ月分貯まった時点で、生存者バイアスを排した
      (「当時APYが低かった/存在しなかったプール」も母集団に含めた)再検証を追加実装
- [ ] `composite_score()`全体(監査数・掲載日数・ハック減点込み)を対象にした
      予測力検証を`src/backtest.py`に追加(現状は`stability_metrics()`のみ)
- [ ] `/hacks`エンドポイントがDeFiLlamaの公開API一覧から将来外れる可能性がある依存リスクを
      定期的に確認(README既記載の懸念事項のフォローアップ)

---

## 優先順位まとめ

1. **フェーズBのユーザータスク**(監査原本の読解・Ember/Unitasの実装アドレス特定・
   `phase_b_status`の`approved`格上げ) — 最優先、ユーザー本人のタスク。
2. フェーズC(少額実行) — 上記が完了し対象プールが`approved`になった後。
3. フェーズD(モニタリング) — Cと並行。日次スナップショット・Phase Bゲート付き
   `scoring.py`定期実行は土台ができている。
4. フェーズE(検証モデル強化)・Phase Bの先送り項目(CI強制・オンチェーン自動検証・
   リスクモデルのプール単位ツリー化) — 急がず中長期で。
